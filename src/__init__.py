"""Portfolio market-risk model.

Marks `src` as a Python package, so modules can be imported as
`from src.var import historical_var` instead of relying on the current
working directory. Kept empty on purpose: import side effects in a
package's __init__ make the code harder to test.
"""
